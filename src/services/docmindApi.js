/** DocMind API — legacy HF Space (single document). */
export {
  API_BASE_URL,
  ApiError,
  getRoot,
  getHealth,
  getStatus,
  uploadPdf,
  askQuestion,
  clearIndex,
  refreshApiState,
  checkHealth,
} from '../lib/api.ts';
