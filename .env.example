# Backend — Hugging Face Space (chamada direta do browser)
VITE_API_URL=https://salmeida-my-rag-chatbot.hf.space

# App display name (optional)
VITE_APP_NAME=DocMind

# Opcional: mock local (npm run dev:all) — use VITE_API_URL=/api e:
# VITE_API_PROXY_TARGET=http://localhost:3001
# PORT=3001
